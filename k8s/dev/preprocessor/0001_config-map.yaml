apiVersion: v1
kind: ConfigMap

metadata:
  name: ap-config
  namespace: dev

data:
  S3_URL: "http://s3-0.s3:9000"
  KAFKA_BOOTSTRAP_SERVERS: '["kafka-0.kafka:9092"]'
  KAFKA_CONSUMER_GROUP: "audio_preprocessor"
  LOG_LEVEL: "debug"