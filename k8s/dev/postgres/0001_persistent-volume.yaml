apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-local-pg
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /var/lib/k3s-volumes/local
    type: DirectoryOrCreate